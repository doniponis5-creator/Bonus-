# SBonus+ TO'LIQ AUDIT — 11.06.2026

> Qamrov: 1C integratsiya, GreenAPI/WhatsApp, cron'lar, kampaniyalar, client kabinet dizayni.
> Prod holati: `api.smartcentr.store/health` → **healthy** (api ✅ database ✅ redis ✅)

---

# 1-QISM. KRITIK XATOLAR (darhol tuzatish kerak)

## 🔴 K1. `/webhook/1c/expenses` — umuman himoyasiz + ichida crash bor
**Fayl:** `app/api/v1/webhook.py:965-1017`

- Boshqa barcha 1C endpointlardan farqli, bu endpoint `_security_check` ni **chaqirmaydi**: IP tekshiruvi yo'q, HMAC yo'q, API key yo'q. Internetdagi istalgan odam soxta xarajat (Expense) yuborib, P&L hisobotlaringizni buzishi mumkin.
- **Latent bug:** 1008-qatorda `datetime.now(timezone.utc)` ishlatiladi, lekin `datetime`/`timezone` faylda **import qilinmagan** → `"month"` siz kelgan item 500 xato beradi.
- Pydantic schema yo'q — raw JSON, validatsiyasiz, manfiy amount qabul qilinadi.

**Yechim:** `await _security_check(...)` qo'shish + schema, yoki endpointni o'chirish.

## 🔴 K2. Campaign runner — bitta ulkan tranzaksiya: restart = HAMMAGA QAYTA yuboriladi + bonus 2x
**Fayl:** `services/campaign_runner.py` (flush bor, commit yo'q) + `api/v1/campaigns.py:197`

- 2000 kishilik kampaniya = **2+ soat bitta ochiq DB tranzaksiya**. Commit faqat oxirida.
- Konteyner restart bo'lsa (deploy, crash — yoki hujjatlashtirilgan "to'xtatish usuli" `docker compose restart api`!): `status="sent"` belgilari **rollback** bo'ladi, lekin WhatsApp xabarlari allaqachon yetib borgan.
- Bundan ham yomoni: xato bo'lsa campaign **PROCESSING → PENDING** ga qaytariladi (campaigns.py:203-211), ertasi 09:00 cron uni qayta oladi → **hamma qabul qiluvchiga xabar HAM bonus QAYTA** beriladi. Bu aynan `wa_sleeping_trigger` dagi 800+ spam insidenti klassi.
- Qo'shimcha: ochiq tranzaksiya `bonus_accounts` qatorlarini kampaniya tugaguncha **lock** qilib turadi → kampaniya paytida POS/1C earn so'rovlari osilib qoladi (timeout).

**Yechim:** har batch'dan keyin `commit()` + startup'da PROCESSING'da tiqilib qolgan kampaniyalarni recovery (resume yoki FAILED — hech qachon PENDING'ga reset qilmaslik).

## 🔴 K3. `/webhook/greenapi` (kiruvchi WA) — autentifikatsiyasiz → sizning WhatsApp raqamingizdan spam yuborish mumkin
**Fayl:** `api/v1/webhook.py:885-958`

- GreenAPI webhook token tekshiruvi yo'q. Istalgan odam soxta `incomingMessageReceived` POST qilib, chatbot orqali **istalgan raqamga sizning biznes-akkauntingizdan xabar yubortira oladi** → WhatsApp raqami ban bo'lish xavfi.
- Rate limit (30/min) nginx ortida bitta global bucket — himoya emas.
- **Bonus bug:** 946-qator `logger.error(...)` — `logger` faylda umuman aniqlanmagan → chatbot xatosi `NameError` → 500 → GreenAPI webhook'ni qayta-qayta yuboradi.

**Yechim:** GreenAPI webhook token (header) tekshiruvi + `logger` import qilish.

## 🔴 K4. `/webhook/1c/spend` idempotent EMAS — 1C retry = balansdan 2 marta yechiladi
**Fayl:** `api/v1/webhook.py:296-304`, `services/bonus.py:319`

- `earn` da chek dublikati tekshiriladi (yaxshi), lekin `spend` o'z receipt'ini `SPEND-{uuid}` qilib generatsiya qiladi — 1C cheki faqat note'da. Dublikat tekshiruvi **yo'q**.
- 1C timeout'da retry qilsa (M3 tufayli timeout ehtimoli yuqori) — mijoz balansi **ikki marta** yechiladi.

**Yechim:** `receipt_number = f"SPEND-{body.receipt_number}"` saqlash + dublikatda 409 (earn'dagidek).

---

# 2-QISM. YUQORI xavf (shu hafta ichida)

## 🟠 Y1. Bonus kuyish ogohlantirishi — bitta mijozga 30 kun ketma-ket HAR KUNI xabar
`tasks/expiration.py:117-153` — `warn_expiring_bonuses` (10:00 cron):
- Cooldown/dedup YO'Q, Notification yozilmaydi → 30 kunlik oynada turgan mijoz **har kuni** bir xil xabar oladi.
- Limit yo'q va xabarlar `asyncio.create_task` bilan **bir vaqtda burst** yuboriladi (3s interval yo'q) — WA-ban xavfi.
- 11:30 dagi `expiry_personal_alert` bilan bir kunda ikkalasi kelishi mumkin.

**Yechim:** comeback_reminder pattern'ini ko'chirish (Notification cooldown + max/run + 3s interval).

## 🟠 Y2. "Yuborish" tugmasi double-click = butun kampaniya 2x
`api/v1/campaigns.py:225-237` — `send_campaign_now` da `FOR UPDATE` yo'q. Ikki parallel so'rov ikkalasi PENDING ko'radi → ikkita background task → hammaga 2x xabar + 2x bonus.
**Yechim:** atomik claim: `UPDATE bonus_campaigns SET status='processing' WHERE id=:id AND status='pending'` + rowcount tekshirish.

## 🟠 Y3. Refund fallback — topilmagan chekda XARID SUMMASINI bonusdan yechadi
`api/v1/webhook.py:367-369`: original EARN topilmasa `refund_bonus = body.refund_amount` — 10 000 som qaytarish = mijozdan 10 000 BONUS yechiladi (2-7% o'rniga). `refund_ratio` ham 1.0 da cap qilinmagan.
**Yechim:** fallback'da tier % bo'yicha hisoblash yoki rad etish; ratio'ni `min(ratio, 1)`.

## 🟠 Y4. Admin WA-broadcast — HTTP so'rov ichida soatlab sinxron loop, tracking yo'q
`api/v1/wa_broadcast.py:114` + `services/wa_broadcast.py:168-186`: "all" segment = soatlab davom etuvchi HTTP so'rov; nginx timeout beradi, admin xato ko'radi, **server esa yuborishda davom etadi**; qayta bosish = hammaga yana. Notification yozilmaydi. Bu 800+ spam insidentining aynan anatomiyasi.
**Yechim:** kampaniya mexanizmi orqali yuborish yoki background task + per-recipient tracking.

## 🟠 Y5. IP whitelist nginx ortida ishlamayapti
`webhook.py:97` — `request.client.host` nginx ortida doim nginx IP. Real 1C IP hech qachon tekshirilmaydi (HMAC majburiy bo'lgani uchun POST'lar himoyalangan — lekin "whitelist" qatlami o'lik). Rate limiter esa aksincha — mijoz yuborgan `X-Forwarded-For`ning **birinchi** elementiga ishonadi (`core/rate_limiter.py:114-120`) → limit bypass.
**Yechim:** nginx'dan `X-Real-IP` (trusted proxy) — ikkala joyda ham bir xil.

---

# 3-QISM. O'RTA xavf (rejaga kiritish)

| # | Muammo | Joy |
|---|--------|-----|
| M1 | WhatsApp yuborilmasa ham `status="sent"` yoziladi — `_send_and_log` return'ni tekshirmaydi; retry ishlamaydi, statistika yolg'on. Kampaniya xabarlari Notification'ga umuman yozilmaydi (audit trail yo'q) | `smart_notifications.py:100-109`, `campaign_runner.py:217-222` |
| M2 | `notification_retry` timeout'da yetib borgan xabarni dublikat yuborishi mumkin (max 3x); 50 ta xabar intervalsiz burst | `tasks/notification_retry.py:48-51` |
| M3 | WhatsApp HTTP chaqiruvi (10s) **commit'dan OLDIN**, `FOR UPDATE` lock ushlagan holda → 1C timeout + commit yiqilsa mijoz "bonus keldi" deb xabar olgan, lekin bonus yo'q | `bonus.py:215-223, 326-334` |
| M4 | Refund kamchiliklari: sarflangan (SPEND) bonuslar qaytarilmaydi; bitta chekka 2-chi/qisman refund 500 beradi; `REFUND-`+50 belgi = overflow; tier pastga qayta hisoblanmaydi | `webhook.py:325-415` |
| M5 | GET `/1c/customer`, `/1c/check-spend`: kill-switch DB'dan emas `.env`dan; API key `!=` bilan (timing-safe emas); HMAC secret'ni key sifatida qayta ishlatish | `webhook.py:531-606` |
| M6 | 1C orqali registratsiyada referral **hech kimga to'lanmaydi** (`referred_by` set qilinadi, bonus yo'q; keyin `apply_referral` REFERRAL_ALREADY_USED deydi) | `webhook.py:471-489` |
| M7 | Auto-coupon bonusda yuqori cap yo'q — avg chek 200k bo'lsa 18 200 som kupon avtomatik | `auto_coupon.py:158-164` → `AUTO_COUPON_MAX_BONUS` qo'shish |
| M8 | Magic-link tokenlar 2 joyda ochiq saqlanadi (`customer_auth_tokens.token` + `notifications.message`) — DB/admin o'qigan odam mijoz kabinetiga kira oladi. Kampaniya tokenlari 7 kun yashaydi va hech qachon tozalanmaydi | hash saqlash + 02:00 cron'da purge |
| M9 | HMAC'da replay himoyasi yo'q (timestamp/nonce yo'q) — ushlangan so'rov qayta yuborilishi mumkin (spend uchun xavfli, K4 bilan birga) | `webhook.py:55-58` |
| M10 | Cron to'qnashuvlari: 11:00 (churn 60 + payshanba auto_coupon 50) + 11:10 + 11:30 — real WA interval 3s → ~1.5s ga tushadi | `main.py` — slotlarni surish |

**Past (LOW) xavflar:** shop order WA xabari hech qachon ishlamagan (`shop.py:354` — argument yetishmaydi, except yutadi); o'lik kod (`greenapi.py`, chaqirilmaydigan funksiyalar); `normalize_phone` "abc" ni qabul qiladi; products-sync'da bitta dublikat SKU = butun 5000 lik batch rollback; admin-panel refund'lari post-purchase followup'ni to'xtatmaydi (boshqa receipt format); `asyncio.create_task` natijalari saqlanmaydi (GC yutishi mumkin).

**Yaxshi qilingan (tasdiqlangan):** HMAC majburiy va fail-closed, timing-safe; pul hisoblari to'liq Decimal; 30% cap + `FOR UPDATE` — balans manfiy bo'lolmaydi; transactions immutable trigger; OTP oqimi (HMAC-hash, constant-time) — zo'r; comeback_reminder — namunali pattern; kupon aktivatsiyasi (FOR UPDATE, "xarid kupondan keyin" qoidasi) to'g'ri.

---

# 4-QISM. DIZAYN AUDITI — "Professional va sodda"

## Diagnoz: nega "PRO" ko'rinmayapti

Token tizimi (`globals.css:6-77`) aslida yaxshi qurilgan — **muammo: hech kim ishlatmaydi**:

| O'lchov | Tizimda | Realda | Natija |
|---------|---------|--------|--------|
| Ranglar | ~20 token | **52 xil raw hex**, 649 inline style | 5 xil sariq, 4 xil qizil, 5 xil yashil. `#8899aa` 40 marta — palitrada umuman yo'q |
| Shrift o'lchami | 5 klass | **24 xil px** (9.5…48) | Shkala yo'q |
| Radius | 6 token | **18 xil qiymat** | Tartibsiz |
| Font-weight | — | 800×39, 900×5 | Hammasi qalin → hech narsa qalin emas |
| Gradient | — | **40 ta** linear-gradient | Kazino effekti |

## "Bolalarcha" his qildiradigan aniq sabablar

1. **Glow hamma joyda** — sariq nur tab'da, progress'da, modallar `pulse-glow` bilan. Glow = kazino, fintech emas.
2. **5 xil tugma tili** — sariq-to'q sariq gradient, 2 xil yashil gradient, Google-ko'k, flat qizil. Top brendlarda CTA rangi BITTA.
3. **Ochilishda modal hujumi** — "Вас ждёт удача!" pulsatsiyali fullscreen + Gamification onboarding overlay. 4 xil feedback pattern (2 toast + 2 banner + 3 overlay).
4. **Emoji UI sifatida** — 💡 📱 😔 Lucide ikonkalar yonida. Aralashtirish = noprofessional.
5. **Bosh sahifada 4 ta "qahramon" raqam** — balans (sariq 36px) ostida qarz (QIZIL 28px — diqqatni yutadi!), yashil/to'q sariq statlar, sariq referral kod. Hammasi baqiradi = hech kim eshitilmaydi.
6. **Giper-matn** — "КРУТИТЬ!", "Невероятно!", "станьте легендой магазина". Professional ohang — sokin.
7. Mijozga ichki jargon: "Рассрочка (1C)", "Синхр: {timestamp}".
8. Tab bar: 6 tab × ~70px, 9.5px label, aktiv holatda **5 ta signal birdan** (glow pill + border + shadow + rang + weight 500→800 sakrashi → label kengligi o'zgaradi).

## Yechim — 10 qadam (prioritet tartibida)

1. **Raw hex taqiqlash**: bitta sariq `--accent`, bitta `--danger`, bitta `--success`, 2 kulrang. Mexanik almashtirish: `#8899aa→var(--text-2)` (40×), `#ff4d4d/#ef4444/#F09595→var(--danger)`, `#22c55e/#10b981/#5DCAA5→var(--success)`. Bug fix: `login/page.tsx:172` `var(--card-border)` → `var(--border)` (token mavjud emas!).
2. **Tugmani tekislash**: `.btn-primary` = solid sariq, gradientsiz, glowsiz. BARCHA "olish/aktivlash/aylantirish" tugmalari shu klassdan.
3. **Balans — yagona qahramon**: 40-44px. Qarz kartasi → bir qatorli row (qizil faqat overdue'da). 2 ta stat kartani profil/tier sheet'ga ko'chirish. Bosh sahifa ≤5 blok.
4. **QR'ni hero'ga ko'tarish** — do'kondagi №1 amal, hozir 5-o'rindagi kulrang tugma. BalanceCard ichiga/ostiga accent qilib qo'yish.
5. **6 tab → 4 tab**: Цели/Удача/Рейтинг → bitta «Клуб» tab (ichida segmented control). Natija: `Главная · Клуб · Бонусы · Профиль`. Label 11px bo'ladi, aktiv holat = FAQAT rang (glow pill o'chadi, weight doim 600). Apple Wallet/Revolut aynan shunday.
6. **Tipografika: 4 o'lcham, 2 vazn**: Display 32-40/700 (faqat balans), Title 17/600, Body 14/400-500, Caption 12/500. 800/900 (44 ta) → 700. Inter'ni `next/font` orqali (hozir render-blocking Google Fonts).
7. **2 radius**: 16px karta/input, 999 pill. 18 ta qiymatni supurish.
8. **De-gradient**: 40 → ~5. Aurora fon → flat `--bg`; `card-accent` gradient → 1px accent border; barcha toast/win gradientlari olib tashlanadi.
9. **Bitta feedback tizimi**: mavjud bottom-sheet (zo'r ishlangan) universal sirt + bitta toast. "Вас ждёт удача!" modal → balans ostida bir qatorli banner «У вас 2 вращения →».
10. **Emoji → Lucide, matn → sokin**: 💡→Sparkles, 😔→AlertCircle; "КРУТИТЬ!"→"Крутить". Pinch-zoom qaytarish (`userScalable:false` — accessibility buzilishi), `#475569` 13px (kontrast AA dan past) → `--text-3`.

## Saqlab qolish kerak (yaxshi ishlangan)
Count-up balans (reduced-motion bilan), skeleton loading, OTP input UX (top daraja), bottom-sheet'lar (grab handle + spring), g'ildirak engineering'i (canvas + GPU), staggered fade-up (40-60ms), kupon "ticket" dizayni, safe-area handling.

**Bir gaplik brief:** qora-shisha estetika va spring-motion qoladi, lekin sariq — YAGONA ovoz (qolgani neytral), 4 tab, 4 shrift o'lchami, 2 radius, 0 emoji, 0 glow. Bu ham "PRO", ham "sodda".

---

# 5-QISM. TAVSIYA ETILGAN TARTIB

**1-bosqich (bugun-ertaga, backend xavfsizlik):**
1. K1 — `/1c/expenses` himoyalash (+import fix)
2. K3 — greenapi webhook token + `logger` import
3. K4 — spend idempotency
4. Y2 — kampaniya atomik claim
5. Y3 — refund fallback/ratio cap

**2-bosqich (shu hafta, spam-xavf):**
6. K2 — campaign runner per-batch commit + PROCESSING recovery
7. Y1 — expiration warning dedup + interval
8. Y4 — wa-broadcast'ni background + tracking'ga o'tkazish
9. M1 — send natijasini hurmat qilish + kampaniya Notification'lari

**3-bosqich (dizayn — alohida sessiya):**
10. Token enforcement + tugma + tipografika (1, 2, 6, 7-qadamlar) — eng katta vizual effekt
11. Bosh sahifa ierarxiyasi + QR (3, 4)
12. Tab bar 6→4 (5) — bunga mockup ko'rsatib, tasdiqdan keyin
13. Qolganlari (8, 9, 10)

**Eslatma:** yangi fichalar prodda hali OFF: `BASKET_BONUS_TIERS`, `AUTO_COUPON_ENABLED`, `POST_PURCHASE_FOLLOWUP_ENABLED`. Yoqishdan oldin M7 (auto-coupon max cap) ni qo'shgan ma'qul.
