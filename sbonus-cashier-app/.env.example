# API адрес backend (FastAPI). В dev — IP вашей машины в локальной сети,
# чтобы Expo Go на телефоне мог достучаться. localhost работает только в симуляторе.
EXPO_PUBLIC_API_URL=http://192.168.0.121:8000
