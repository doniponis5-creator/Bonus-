/**
 * index.tsx — unused when App.tsx is the entry point.
 * Kept as a no-op to avoid Metro resolution errors.
 */
import { View } from 'react-native';
export default function Index() {
  return <View />;
}
